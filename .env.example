# Toggle between 'offline' (runs heuristics on client) and 'backend' (runs on Node server)
EXPO_PUBLIC_AI_MODE=offline
# Ensure the port matches your backend/server.ts configuration
EXPO_PUBLIC_AI_BACKEND_URL=http://localhost:3000

# Phase 10: Intake feature flags (default OFF)
EXPO_PUBLIC_FEATURE_INTAKE_DEEPLINK=false
EXPO_PUBLIC_FEATURE_INTAKE_SHARE=false
EXPO_PUBLIC_FEATURE_INTAKE_CLIPBOARD=false
