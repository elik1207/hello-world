PORT=3000

# AI Extraction Configuration
# Choose between "deterministic" (local heuristic fallback) or "llm" (OpenAI structured JSON provider)
AI_PROVIDER=deterministic

# Optional: Configuration for LLM Mode (Only active if AI_PROVIDER=llm)
AI_API_KEY=your_openai_api_key_here
AI_MODEL=gpt-4o-mini
AI_TIMEOUT_MS=8000
AI_MAX_QUESTIONS=1
AI_LOG_LEVEL=info
